tailwind.config = {
    theme: {
        extend: {
            fontFamily: {
                sans: ['Inter', 'sans-serif'],
            },
            colors: {
                'primary': '#4338ca', // Deep Indigo (700)
                'primary-light': '#6366f1', // Lighter Indigo (500)
            }
        }
    },
    darkMode: 'class', 
}
